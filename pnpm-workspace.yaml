packages:
  - "apps/*"
  - "packages/*"

# Effect packages must stay version-aligned — pin them here so every
# workspace consumes the same versions via `catalog:`.
catalog:
  "effect": "^3.21.4"
  "@effect/platform": "^0.96.2"
  "@effect/platform-node": "^0.107.0"
