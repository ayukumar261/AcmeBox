meta {
  name: Setup Payment Method
  type: http
  seq: 3
}

post {
  url: {{baseUrl}}/customers/{{ordCustomerId}}/payment-methods
  body: json
  auth: none
}

body:json {
  {
    "brand": "mastercard",
    "last4": "5454",
    "expiryMonth": 6,
    "expiryYear": 2031
  }
}

script:post-response {
  bru.setVar("ordPaymentMethodId", res.getBody().id);
}

assert {
  res.status: eq 201
}

tests {
  test("creates the fixture payment method", function () {
    expect(res.getStatus()).to.equal(201);
    expect(res.getBody().id).to.match(/^pm_/);
  });
}
